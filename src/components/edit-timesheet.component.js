import React, { Component } from "react";

export default class CreateTimeSheet extends Component {
  render() {
    return (
      <div>
        <p>React Create TimeSheet Component!</p>
      </div>
    );
  }
}